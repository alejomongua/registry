# Example environment variables for docker-registry
# Copy this file to .env and edit values as needed

# Domain that will be used to serve the registry (used in nginx/registry.conf)
DOMAIN=tu-dominio.com

# External HTTPS port for NGINX (host port)
NGINX_PORT=443

# Internal registry port (container exposes 5000 by default; keep for reference if you change)
REGISTRY_PORT=5000
